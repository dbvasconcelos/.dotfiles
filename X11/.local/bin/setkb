#!/bin/sh
# Keyboard settings

xset r rate 210 40 # Faster cursor
setxkbmap -layout "us" -variant "intl" -option caps:super # CapsLock is Super
killall xcape 2>/dev/null ; xcape -e 'Super_L=Escape' # Super pressed once is ESC
